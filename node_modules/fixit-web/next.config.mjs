/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow proxying API routes to our API service if necessary, or client connects directly
};

export default nextConfig;
